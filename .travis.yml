language: cpp

compiler:
  - gcc
  - clang

cache:
  - apt
  - ccache

sudo: false

addons:
  apt:
    packages:
      - libdb-dev
      - libreadline-dev
      - libssl-dev
      - libwww-perl
      - xsltproc
      - zlib1g-dev

env:
  - TEST_TARGET=debug
  - TEST_TARGET=release

script:
  - ./infrastructure/travis-build.sh
